-- 8. 
-- a) 
-- myX = 1 :: Int
-- sigmund' :: Int -> Int
-- sigmund' x = myX

-- b) 
-- sigmund' :: Num a => a -> a

-- Solution:
-- Cannot, because myX is Int, so sigmund' must be at least Int -> Int.