-- 4. Categorize each component of the type signature
-- f :: f -> g -> C

-- Solution:
-- 'f' is a fully polymorphic type variable, since it is lowercase.
-- 'g' is a fully polymorphic type variable, since it is lowercase.
-- 'C' is a concrete (nullary) type constructor, since it is uppercase.